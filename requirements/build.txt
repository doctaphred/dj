# Packages that are used to build the project,
# but are not required to run or deploy it.

-r base.txt

flake8==3.0.4
pytest==3.0.3
nose-parameterized==0.5.0

# https://github.com/HypothesisWorks/hypothesis-python
hypothesis==3.6.0
